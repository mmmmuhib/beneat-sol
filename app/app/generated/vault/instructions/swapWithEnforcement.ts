/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const SWAP_WITH_ENFORCEMENT_DISCRIMINATOR = new Uint8Array([
  41, 90, 136, 244, 222, 10, 24, 210,
]);

export function getSwapWithEnforcementDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SWAP_WITH_ENFORCEMENT_DISCRIMINATOR,
  );
}

export type SwapWithEnforcementInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type SwapWithEnforcementInstructionData = {
  discriminator: ReadonlyUint8Array;
  amountIn: bigint;
  minOut: bigint;
};

export type SwapWithEnforcementInstructionDataArgs = {
  amountIn: number | bigint;
  minOut: number | bigint;
};

export function getSwapWithEnforcementInstructionDataEncoder(): FixedSizeEncoder<SwapWithEnforcementInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amountIn", getU64Encoder()],
      ["minOut", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SWAP_WITH_ENFORCEMENT_DISCRIMINATOR,
    }),
  );
}

export function getSwapWithEnforcementInstructionDataDecoder(): FixedSizeDecoder<SwapWithEnforcementInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amountIn", getU64Decoder()],
    ["minOut", getU64Decoder()],
  ]);
}

export function getSwapWithEnforcementInstructionDataCodec(): FixedSizeCodec<
  SwapWithEnforcementInstructionDataArgs,
  SwapWithEnforcementInstructionData
> {
  return combineCodec(
    getSwapWithEnforcementInstructionDataEncoder(),
    getSwapWithEnforcementInstructionDataDecoder(),
  );
}

export type SwapWithEnforcementAsyncInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault?: Address<TAccountVault>;
  amountIn: SwapWithEnforcementInstructionDataArgs["amountIn"];
  minOut: SwapWithEnforcementInstructionDataArgs["minOut"];
};

export async function getSwapWithEnforcementInstructionAsync<
  TAccountOwner extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: SwapWithEnforcementAsyncInput<TAccountOwner, TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SwapWithEnforcementInstruction<TProgramAddress, TAccountOwner, TAccountVault>
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.owner), getAccountMeta(accounts.vault)],
    data: getSwapWithEnforcementInstructionDataEncoder().encode(
      args as SwapWithEnforcementInstructionDataArgs,
    ),
    programAddress,
  } as SwapWithEnforcementInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault
  >);
}

export type SwapWithEnforcementInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  amountIn: SwapWithEnforcementInstructionDataArgs["amountIn"];
  minOut: SwapWithEnforcementInstructionDataArgs["minOut"];
};

export function getSwapWithEnforcementInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: SwapWithEnforcementInput<TAccountOwner, TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): SwapWithEnforcementInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.owner), getAccountMeta(accounts.vault)],
    data: getSwapWithEnforcementInstructionDataEncoder().encode(
      args as SwapWithEnforcementInstructionDataArgs,
    ),
    programAddress,
  } as SwapWithEnforcementInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault
  >);
}

export type ParsedSwapWithEnforcementInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
  };
  data: SwapWithEnforcementInstructionData;
};

export function parseSwapWithEnforcementInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSwapWithEnforcementInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), vault: getNextAccount() },
    data: getSwapWithEnforcementInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
