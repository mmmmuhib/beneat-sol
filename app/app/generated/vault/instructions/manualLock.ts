/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const MANUAL_LOCK_DISCRIMINATOR = new Uint8Array([
  128, 7, 199, 201, 41, 62, 228, 177,
]);

export function getManualLockDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MANUAL_LOCK_DISCRIMINATOR);
}

export type ManualLockInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type ManualLockInstructionData = { discriminator: ReadonlyUint8Array };

export type ManualLockInstructionDataArgs = {};

export function getManualLockInstructionDataEncoder(): FixedSizeEncoder<ManualLockInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: MANUAL_LOCK_DISCRIMINATOR }),
  );
}

export function getManualLockInstructionDataDecoder(): FixedSizeDecoder<ManualLockInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getManualLockInstructionDataCodec(): FixedSizeCodec<
  ManualLockInstructionDataArgs,
  ManualLockInstructionData
> {
  return combineCodec(
    getManualLockInstructionDataEncoder(),
    getManualLockInstructionDataDecoder(),
  );
}

export type ManualLockAsyncInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault?: Address<TAccountVault>;
};

export async function getManualLockInstructionAsync<
  TAccountOwner extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: ManualLockAsyncInput<TAccountOwner, TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ManualLockInstruction<TProgramAddress, TAccountOwner, TAccountVault>
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.owner), getAccountMeta(accounts.vault)],
    data: getManualLockInstructionDataEncoder().encode({}),
    programAddress,
  } as ManualLockInstruction<TProgramAddress, TAccountOwner, TAccountVault>);
}

export type ManualLockInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
};

export function getManualLockInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: ManualLockInput<TAccountOwner, TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): ManualLockInstruction<TProgramAddress, TAccountOwner, TAccountVault> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.owner), getAccountMeta(accounts.vault)],
    data: getManualLockInstructionDataEncoder().encode({}),
    programAddress,
  } as ManualLockInstruction<TProgramAddress, TAccountOwner, TAccountVault>);
}

export type ParsedManualLockInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
  };
  data: ManualLockInstructionData;
};

export function parseManualLockInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedManualLockInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), vault: getNextAccount() },
    data: getManualLockInstructionDataDecoder().decode(instruction.data),
  };
}
